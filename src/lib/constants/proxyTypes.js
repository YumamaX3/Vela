/**
 * Shared proxy types catalog — single source of truth for both routes.
 * Proxy Completion Covenant W1: unified union across all pools.
 */
export const VALID_PROXY_TYPES = ["http", "https", "vercel", "cloudflare", "deno", "socks5"];